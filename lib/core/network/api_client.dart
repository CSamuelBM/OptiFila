import 'package:optifila/core/config/api_config.dart';

class ApiClient {
  /*
  final http.Client _client = http.Client();

  Future<http.Response> get(String endpoint){
      return _client.get(
      Uri.parse('${ApiConfig.baseUrl}$endpoint'),
      headers:{
        'Content-Type' : 'application/json',
      }
    );
  }
  */
}